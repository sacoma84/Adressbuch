<Window x:Class="Adressbuch.Views.ContactView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Adressbuch.Views"
        xmlns:vms="clr-namespace:Adressbuch.ViewModels"
        mc:Ignorable="d"
        Title="ContactView" Height="450" Width="800">
    <!--<Window.DataContext>
        <vms:ContactViewModel/>
    </Window.DataContext>-->
    <Window.Resources>
        <vms:ContactViewModel x:Key="cvm"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dgContactList" ItemsSource="{Binding ContactList, Source={StaticResource cvm}}" SelectedItem="{Binding Path=SelectedContact}">
            
        </DataGrid>
        
        
    </Grid>
</Window>
